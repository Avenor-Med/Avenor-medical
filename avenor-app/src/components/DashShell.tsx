// Superseded by src/layouts/PortalLayout.tsx during the modular refactor.
// Re-exported so any straggling import keeps working; delete after one release.
export { default } from '@/layouts/PortalLayout';
export { StatCard } from '@/components/ui/Card';
